<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Comments</title>
  </head>
  <body>
    <div>
      <div>
        <h1>Here are all the comments</h1>

        <% for(comment of data) { %>

        <p><b><%= comment.username%></b>: <%= comment.comment%></p>

        <form style="display: inline-block" action="comments/<%=comment.id%>/" method="GET">
          <button>View Comment</button>
        </form>

        <form style="display: inline-block" action="comments/edit/<%=comment.id%>/" method="GET">
          <button>Edit Comment</button>
        </form>

        <% } %>
      </div>

      <form action="comments/new" method="GET">
        <button style="margin-top: 50px">Add Comment</button>
      </form>
    </div>
  </body>
</html>
